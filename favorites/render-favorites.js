import { favoritesArray } from '../results/makeFavesArray.js';

const renderFavorites(lineItem, userFavorites);

const li = document.createElement('li');
li.name = 
li.address =
li.phone = 


export default renderFavorites;